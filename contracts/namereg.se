def register(key, value):
    if !self.storage[key] or key == msg.sender:
        self.storage[key] = value
        self.storage[value] = key
        return(1)
    return(0)

def get_nameoraddress(key):
    return(self.storage[key])
